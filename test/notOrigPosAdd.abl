agent Agent {
  position float2 pos;
}

environment {
  max: float2(1),
  granularity: 0.1
}

step step_fn(Agent in->out) {
  add(Agent { pos: float2(0) });
  add(Agent { pos: out.pos });
  add(Agent { pos: in.pos });
}

void main() {}
